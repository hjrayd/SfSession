{% extends 'base.html.twig' %}

{% block title %}List of categories{% endblock %}

{% block body %}
    <h1> List of categories</h1>

        {% for category in categories %}
        <a href="{{ path('show_category', {'id' : category.id} )}}"> {{ category }}</a> <br>
        {% endfor %}
{% endblock %}
