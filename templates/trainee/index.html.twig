{% extends 'base.html.twig' %}

{% block title %}List of trainees{% endblock %}

{% block body %}
    <h1> List of trainees</h1>

        {% for trainee in trainees %}
        
         <a href="{{ path('show_trainee', {'id':trainee.id} )}}">{{ trainee }} </a> ( {{ trainee.phone}} ) <br> 
       
        {% endfor %}
{% endblock %}
